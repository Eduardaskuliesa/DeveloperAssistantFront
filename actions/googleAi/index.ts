import { createCache } from "./createCache";
import { createChat } from "./createChat";
import { getHistory } from "./getHistory";
import { sendMessage } from "./test";

export const googleAiActions = {
  sendMessage,
  createChat,
  getHistory,
  createCache,
};
